# The runtime dependency is a SIBLING CHECKOUT, not a package:
#
#   git clone <harneskills> ../harneskills
#   PYTHONPATH=../harneskills python -m pytest tests/ -q
#
# `pystrider/facts.py` asserts the twelve names it uses on import, so a harneskills
# that has moved fails by name. See the note in pyproject.toml.
#
# textual — drives the Textual app `demos/playground` emits: verified by running it
#           headlessly through textual's App.run_test() Pilot, never by reading the
#           generated source.
textual>=8,<9
