# Runtime dependency is declared in pyproject.toml (universal-graph-machine, installed via
# `pip install -e ../Universal-Graph-Machine -e .`).
#
# ⚠ The sibling checkout is named `Universal-Graph-Machine`, not `ugm` — `../ugm` was this file's
# instruction until 2026-08-18 and it named a directory that does not exist.
#
# Nothing else is needed. This file used to pin `textual`, which drove the emitted Textual app in
# `experiments/strider_app.py`; that probe ran on the engine-2 floor upstream deleted, so it was
# retired and textual lost its only consumer.
