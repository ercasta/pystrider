# Runtime dependency is declared in pyproject.toml (universal-graph-machine, installed via
# `pip install -e ../ugm -e .`). This file pins the extra packages the EXPERIMENTS need.
#
#   textual — drives experiments/app_synthesis.py: the emitted Textual app is verified by driving
#             it headlessly through textual's App.run_test() Pilot.
textual>=8,<9
